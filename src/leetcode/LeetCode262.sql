select t.Request_at as Day, Round(count(if(t.Status like 'cancelled%', true, null))/count(*),2) as 'Cancellation Rate'
from Trips t inner join Users u on u.Users_Id = t.Client_Id and u.Banned = 'No' where t.Request_at between '2013-10-01' and '2013-10-03'group by t.Request_at;